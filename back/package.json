{
  "name": "red-tetris-back",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha --require babel-core/register --reporter spec",
    "coverage": "NODE_ENV=test nyc --include src --all -r text -r lcov -r text --require babel-core/register mocha "
  },
  "author": "chorange",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "http": "0.0.1-security",
    "serve-favicon": "^2.5.0",
    "socket.io": "^4.3.2",
    "ws": "^8.2.3"
  },
  "devDependencies": {
    "babel-core": "^6.26.3",
    "mocha": "^9.1.3",
    "nyc": "^15.1.0"
  }
}
